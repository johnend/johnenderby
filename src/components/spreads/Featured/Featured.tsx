import SpreadContainer from '../_lib/SpreadContainer/SpreadContainer';

const Features = () => {
  return <SpreadContainer spreadNumber={3} spreadTitle='Featured work' tone='ink' />;
};

export default Features;
