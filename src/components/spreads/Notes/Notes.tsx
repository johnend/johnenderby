import SpreadContainer from '../_lib/SpreadContainer/SpreadContainer';

const Notes = () => {
  return <SpreadContainer spreadNumber={5} spreadTitle='Writing' tone='ink' />;
};

export default Notes;
